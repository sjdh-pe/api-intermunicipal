# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=pelivreacesso
DB_USERNAME=
DB_PASSWORD=

# Application Configuration
APP_PORT=3000
APP_PROFILE=dev

# Spring Security
SECURITY_USERNAME=admin
SECURITY_PASSWORD=

# JPA Configuration
JPA_DDL_AUTO=validate
SHOW_SQL=false

# JWT Configuration
JWT_SECRET=
JWT_EXPIRATION=86400000

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10MB
MAX_REQUEST_SIZE=10MB

# Logging
LOG_LEVEL=INFO

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200

# Email Configuration (opcional)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=noreply@sjdh.pe.gov.br